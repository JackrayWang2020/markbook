

# 概述

冒泡：像冒泡一样最大的泡泡先冒出来。。

## 简称智障排序 java

```java

package org.wyl.soulland;

public class BubbleSort {

    public static void main(String[] args) {
        int[] nums = {7, 6, 3, 9, 1, 8};
        bubbleSort(nums);
    }

    public static void bubbleSort(int[] nums) {
        if (nums == null && nums.length == 0) {
            return;
        }
        int le = nums.length;
        if (le == 1) {
            System.out.println(nums[0]);
        }

        for (int i = 0; i < le; i++) {
            sort(nums, 0, le - i);
        }

        for (int n = 0; n < le; n++) {
            System.out.println(nums[n]);
        }

    }

    public static void sort(int[] nums, int left, int right) {
        if (right >= nums.length) {
            return;
        }
        if (nums[left] > nums[right]) {
            int mind = -1;
            mind = nums[left];
            nums[left] = nums[right];
            nums[right] = mind;
        }
        sort(nums, ++left, ++right);
    }
}

```

## 简称智障排序 python

```python
def bubble_sort(nums):
    le = len(nums)
    for i in range(le):
        for j in range(le - i - 1):
            if nums[j] > nums[j + 1]:
                nums[j], nums[j + 1] = nums[j + 1], nums[j]
    return nums


if __name__ == '__main__':
    print("sad")
    pre_nums = [7, 6, 3, 9, 1, 8]
    print("排序前的数组：" , pre_nums)
    post_nums = bubble_sort(pre_nums)
    print("排序后的数组：" , post_nums)


```



## 看来我是越来越菜了 哈哈哈


```java
public class BubbleSort {
    
    // 冒泡排序算法
    public static void bubbleSort(int[] arr) {
        int n = arr.length;
        for (int i = 0; i < n; i++) {
            // 内层循环进行相邻元素比较并交换
            for (int j = 0; j < n - i - 1; j++) {
                if (arr[j] > arr[j + 1]) {
                    // 交换相邻的元素
                    int temp = arr[j];
                    arr[j] = arr[j + 1];
                    arr[j + 1] = temp;
                }
            }
        }
    }

    public static void main(String[] args) {
        // 输入参数
        int[] arr = {7, 6, 3, 9, 1, 8};
        
        // 打印排序前的数组
        System.out.print("排序前的数组: ");
        for (int num : arr) {
            System.out.print(num + " ");
        }
        System.out.println();
        
        // 调用冒泡排序方法
        bubbleSort(arr);
        
        // 打印排序后的数组
        System.out.print("排序后的数组: ");
        for (int num : arr) {
            System.out.print(num + " ");
        }
    }
}

```